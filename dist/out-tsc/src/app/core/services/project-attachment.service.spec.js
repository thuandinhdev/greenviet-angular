import { TestBed } from '@angular/core/testing';
import { ProjectAttachmentService } from './project-attachment.service';
describe('ProjectAttachmentService', function () {
    beforeEach(function () { return TestBed.configureTestingModule({}); });
    it('should be created', function () {
        var service = TestBed.get(ProjectAttachmentService);
        expect(service).toBeTruthy();
    });
});
//# sourceMappingURL=project-attachment.service.spec.js.map